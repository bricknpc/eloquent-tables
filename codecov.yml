coverage:
  status:
    # The repository is held at 100%. Anything less is a regression, not a trend.
    project:
      default:
        target: 100%
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%

comment:
  layout: "condensed_header, diff, files"
  behavior: default
  require_changes: false

ignore:
  - "docs/**"
  - "tests/**"
  - "stubs/**"
